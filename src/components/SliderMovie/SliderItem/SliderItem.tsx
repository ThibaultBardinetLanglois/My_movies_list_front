import './sliderItem.scss';

const SliderItem = ({item}: any) => {

  console.log("item in slider item =>", item);
  return (
    <div className='sliderItem'>
      <p>Item</p>
    </div>
  )
}

export default SliderItem